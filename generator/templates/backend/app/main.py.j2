from fastapi import FastAPI
from .routers import router

app = FastAPI(title="{{ title }}")
app.include_router(router)

@app.get("/")
def root():
    return {"ok": True, "service": "{{ title }}", "docs": "/docs"}
