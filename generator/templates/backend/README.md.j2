# {{ title }} (Generated)

A minimal FastAPI + SQLModel + SQLite backend for **{{ domain }}**.

## Run (Local)
```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000
# Open http://localhost:8000/docs
```

## Run (Docker)
```bash
docker build -t {{ domain }}-backend .
docker run -p 8000:8000 -v $(pwd)/data:/data {{ domain }}-backend
```

## Notes
- DB: SQLite at `/data/app.db`.
- Entities:
{% for m in models %}
  - {{ m.name }} (table: {{ m.table }})
{% endfor %}
- Extra domain endpoint(s):
{% for e in extras %}
  - {{ e.name }}: {{ e.description }}
{% endfor %}
